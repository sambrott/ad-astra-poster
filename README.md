# Ad Astra — poster

Static site (no build step). Deploy the repository root to any static host: **Netlify**, **Vercel**, **Cloudflare Pages**, or **GitHub Pages** (publish `/` with `index.html` as the entry).

Local preview: open `index.html` in a browser or run `python3 -m http.server` from this directory.
